// Widget tests here need to mock Firebase before pumping OhThePlacesIveBeenApp
// (see https://pub.dev/packages/firebase_auth_mocks). Placeholder until that's wired up.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
